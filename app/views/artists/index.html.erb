<div class="container">
  <h1>Artistas</h1>
  <div class="row">
    <% @artists.each do |artist| %>
    
    <div class="col-md-4">
      <div class="thumbnail">
        <img src="<%= artist.image_url %>" alt=""> </li> 
        <h3><%= link_to artist.name, artist %></h3>
      </div>
    </div>
    
    <% end %>
  </div>
</div>




